#include "base/time.h"

#include <gtest/gtest.h>
#include <glog/logging.h>

namespace base {

TEST(Time, Basic) {
//  LOG(INFO) << "Now0: " << Time::Now().Format("%Y-%m-%d %H:%M:%S");
//  LOG(INFO) << "Now1: " << Time::Now().Format("%Y%m%H%M%S");
  EXPECT_TRUE(true);
}

} // namespace base
